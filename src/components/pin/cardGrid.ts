// A ranked grid of pin cards, read row by row. Columns are a fixed width so a
// card is the same size wherever the grid appears; phones get one full column.
export const CARD_GRID = 'grid grid-cols-[repeat(auto-fill,22rem)] items-start gap-2.5 max-sm:grid-cols-1';
