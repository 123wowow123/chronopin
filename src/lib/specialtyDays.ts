// A specialty day ("National Peanut Day") as a tag shows it: its English
// name, which the tag searches for, and its name in the page's language.
export type SpecialtyDay = { name: string; label: string };
