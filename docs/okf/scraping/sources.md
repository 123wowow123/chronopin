---
type: Reference
title: Source-by-source fetching notes
description: What works and what is blocked for each kind of source and site, with the working workaround - so a scrape, by the API or by hand, does not rediscover it.
resource: ../../../src/server/scrape/sourceText.ts
tags: [scraping, fetching, blocked, workarounds]
generated: { by: claude-code/claude-opus-5, at: 2026-09-19T20:00:00Z }
---

Kept current from real runs. See [Learnings](learnings.md) for the dated log; add a row here when a site's behaviour becomes known.

# How the app fetches

`sourceKind(url)` picks `youtube`, `tweet`, `podcast`, `pdf` or `web` from the host and path ([Fetch a link's text](../pipeline/fetch-link-text.md)); a `.pdf` path is a `pdf`, and a page that turns out to be one is caught by its bytes (below). A web page is fetched plainly first (15 seconds, HTML converted to text); under 500 characters it is loaded in headless Chrome instead. The scraper always renders in Chrome, with the user agent `HeadlessChrome` replaced by `Chrome` because Cloudflare's challenge holds the former (help.openai.com among others), keeps the tab on the page asked for (redirects and script navigation away are blocked), presses PageDown and scrolls to wake lazy players, then reads body text, images, embeds and headings.

# By site

| Source | Works | Blocked or unreliable | Do this |
| --- | --- | --- | --- |
| **Wikipedia and Wikimedia** | Plain `curl` and the API; `og:image` gives the poster or lead photo | The image CDN answers 429 to a client with a browser-like User-Agent. A **year-less title silently redirects to the wrong year**: `State_visit_by_Xi_Jinping_to_the_United_States` answers 200 and serves the **2015** visit, so a reference taken on the status code alone cites the wrong event | Send a User-Agent that names the client and a contact (`ChronoPin/1.0 (https://chronopin.com; tech@chronopin.com) image fetch`): the same image is 200 with it and 429 without. The downloader does this for wikimedia.org and waits out a `Retry-After` of up to 90 seconds once. Always grep the fetched `<title>` for the year you meant before citing an article |
| **Commons `imageinfo`** | `list=search&srnamespace=6` then `prop=imageinfo&iiprop=url|size|extmetadata` gives the file, its pixel size and its licence in two calls | The `url` it returns has `?utm_source=commons.wikimedia.org&utm_campaign=imageinfo&utm_content=original` appended, and free-text search is phrasing-sensitive - "Chinese spy balloon 2023 Montana" returns nothing while "balloon high altitude 2023 United States surveillance" returns the whole DoD set | **Split the url on `?` before storing it as `originalUrl`**, or the CDN-suffix and difference-hash dedupe sees a different file. Try two or three phrasings before concluding Commons has nothing, and read `extmetadata.ImageDescription` on any file whose name is a serial (`P20260428DT-2061.jpg` is a State Arrival Ceremony on the South Lawn) |
| **history.state.gov** (Office of the Historian) | Plain `curl`. The Milestones essays quote their own primary text, and `/historicaldocuments/frus<years>v<vol>/d<n>` serves the document itself - the Shanghai Communiqué is `frus1969-76v17/d203` | The Milestones series carries a banner saying it "has been retired and is no longer maintained" | The best single source for US foreign-policy history to 1980, and it quotes the sentences a date reasoning wants. Note the retirement when citing it, and prefer the FRUS document over the essay where both cover the point - **a document's own dateline beats the popular date** (FRUS dates the Shanghai Communiqué 27 February 1972; it is usually given as the 28th) |
| **eia.gov** (`dnav/pet/hist/LeafHandler.ashx`) | Plain `curl`. The page a series' public URL points at carries its **whole history** as an HTML table - Year-Month rows with (MM/DD, value) pairs across the weeks - back to 1982 for the SPR, with the release and next-release dates at the foot | The v2 API (`api.eia.gov`) needs a registered key and answers empty without one, and the "series history" download is Excel BIFF (`.xls`), not CSV. In the table a withheld week is `W` and a **December row's January week belongs to the next year**, because a week is dated by the Friday it ends on | Parse the page - no key, no spreadsheet dependency (`src/server/eiaSeries.ts`). Attach a series to pins with `npm run series:attach -- --tag SPR --series WCSSTUS1`; the pin stores only the id and the numbers are fetched on view |
| **energy.gov** | Plain `curl` with a browser UA reads every press release, history page and site page | The Office of Petroleum Reserves news listing (`/hgeo/opr/listings/office-petroleum-reserves-news`) **404s**, and every release's `og:image` is the DOE seal or `white-fallback_0.png`, so a page gives no usable picture. A release's *slug* carries no date: `/ceser/articles/us-department-energy-announces-new-solicitations-purchase-oil-strategic-petroleum` is **July 2024**, though a search summary dated it May 2026 | Walk the series through each release's *View Previous/Next Press Release* links at the foot, or from the office's landing page (`/hgeo/opr`). Read the page's own dateline before trusting a search result's date. Take pictures from Commons instead: the *United States Strategic Petroleum Reserve NNN.jpg* series names its site in `extmetadata.ImageDescription`. A `/node/NNNNNN` URL works but carries a `<link rel="canonical">` to the readable path - store that one |
| **presidency.ucsb.edu** (American Presidency Project) | Plain `curl` with a browser UA; `/advanced-search?field-keywords=...&from[date]=...&to[date]=...` fetches and lists document slugs | - | Where a pre-internet White House event gets its **hour**: the `Note:` line at the foot of a document gives the time and place ("the President spoke at 10:12 a.m. on the South Lawn of the White House"). Use the advanced-search URL to find the slug rather than spending a WebSearch |
| **bis.doc.gov** | Nothing for a press release | A `/index.php/documents/about-bis/newsroom/press-releases/<id>-.../file` URL answers **200 with ~76KB of the current BIS homepage** - an aggregator redirect, not a block page, so `looksBlocked` does not catch it | Go to the Federal Register: `GET /api/v1/documents.json?conditions[term]=...&conditions[publication_date][gte]=...` finds the rule in one call, and the document page's **DATES** section is the authoritative effective date (the October 2022 chip controls came in on 7, 12 and 21 October, not one date) |
| **defense.gov, northcom.mil** | Nothing | **403 to `curl`** with a browser UA on every news, release and transcript path tried | Take the Pentagon's statement from Air & Space Forces Magazine, which carries it verbatim and adds the operational detail no release has (the AIM-9X, the 58,000-foot launch altitude, the 2:39 p.m. Eastern shot) |
| **House and Senate member sites** (`pelosi.house.gov`) | A member's own press releases, still served years after they left the leadership | A near-miss slug 404s (`pelosi-statement-on-her-visit-to-taiwan` is nothing; `pelosi-congressional-delegation-statement-on-visit-to-taiwan` is the 2 August 2022 arrival statement) | Find the exact slug by search, not by guessing from the headline |
| **ait.org.tw** (American Institute in Taiwan) | `/policy-history/taiwan-relations-act/` | The long `/our-relationship/policy-history/key-u-s-foreign-policy-documents-region/...` path **404s with a 143KB body**, so a byte count says nothing | Check the fetched `<title>` rather than the response size; the short path serves the same document |
| **Wikipedia election calendars** | `List of elections in <year>` is populated and footnotes each date to the electoral commission | `<year> national electoral calendar` is a stub - its month sections are empty | Take the list page as wikitext and read `election_date` from each election's own infobox; an election with no article of its own is not sourceable |
| **Commons building searches** | `incategory:"<building>"` scoped searches, and the building article's `pageimages` on the **local-language** Wikipedia | Free-text search drifts to another country for generic names ("White House Bishkek" -> the US and Moscow; "Parliament Buildings Nairobi" -> Hungary, British Columbia); `pageimages` on a legislature article returns a seal or a seat-composition SVG | Scope by category, filter the file title for the city or country, then pick by pixel size; fall back to the square or plaza in front of the building |
| **vaalit.fi** (Finnish Ministry of Justice) | WebFetch reads the full statutory election schedule | **403 to `curl`** with any UA tried | Cite it as a reference on the strength of the WebFetch read; do not write a wiki from a blocked capture |
| **skao.int** | PDFs under `/sites/default/files/documents/` with a browser UA, read by `pdftotext` (one timeline per telescope: `Sciops_timeline_low_ppt.pdf`, `Sciops_timeline_mid_ppt.pdf`) | The HTML pages **403 to both `curl` and WebFetch** | Go straight to the PDFs; they carry the AA1/AA2/AA*/Cycle 0 dates the HTML pages describe |
| **federalreserve.gov FOMC calendar** | The page lists every meeting date for years ahead | **No per-meeting URL until a meeting has happened** - future meetings are table rows only, so a batch of pins off it would share one `sourceUrl` and 409 | Use the calendar as the date's reference and take the per-meeting `sourceUrl` from Kalshi's `KXFEDDECISION` series, whose strike times match it |
| **Kalshi `KXFEDDECISION`** | One open event per FOMC meeting (`KXFEDDECISION-27JAN` ...), keyless, with nested markets, `*_dollars` prices and `volume_fp` | kalshi.com 429s to `curl`, as ever | Verify a link with `parseMarketUrl` + `oddsFor` in a `tsx` script; the 2027 books are thin ($1.4k-$23.5k) so quote the volume |
| **whitehouse.gov briefings** | Everything under `briefings-statements/<yyyy>/<mm>/<slug>/` reads with a plain browser UA. A `congressional-bill-h-r-NNNN-signed-into-law/` statement gives the weekday, date, bill number, the act's name and what it funds and until when, and its **Related** list indexes the neighbouring signings. A **state visit** statement gives the official hour-by-hour schedule - which day the President meets the guest at Joint Base Andrews, the arrival ceremony, the military review and flyover, the state dinner and the departure | Its `og:image` is the generic `WH47-Social-Share-Card.jpg`, never the story's own picture | The primary source for the date a funding bill became law or a state visit's schedule, better than any news copy. Walk the Related list to collect a run of signings in one fetch; get pictures elsewhere |
| **thehill.com** | Nothing | A **HUMAN Security** challenge: 403 to WebFetch, and an "Access to this page has been denied" shell to `curl` with a browser UA | Take the same story from The Center Square, Federal News Network (AP copy) or the outlet's own release; do not wiki the challenge page |
| **reuters.com** | **The app's own headless scraper reads the whole article**, byline, dateline, timestamps and the tickers Reuters tags inline (`Amazon (AMZN.O)`) | **401 to `curl`** with a browser UA (a 771-byte body) and "unable to fetch" to WebFetch - so a session's own tools see nothing | Do not judge Reuters by `curl`. Let the pipeline fetch it: `GET /api/scrape`, or post the pin and read the captured text back out of `npm run wiki:export`. While working by hand beforehand, `WebSearch` the headline - the same wire copy is republished in full by ABC News (the AP version), usnews.com and mvariety.com - and keep the Reuters URL as `sourceUrl` under the IMDb rule |
| **scmp.com** | WebFetch reads the article in full, including detail the wire copy drops (the expected arrival time of a state visit); the app's headless scraper reads it too (see the robotics-newsroom row) | **403 to `curl`** with any UA tried, so a link checked by status code alone looks dead when it is not | Cite it on the strength of the WebFetch read, as with vaalit.fi and ebu.ch; do not drop an SCMP reference because `curl` 403s |
| **fmprc.gov.cn** (Chinese foreign ministry) | Plain `curl` **and** WebFetch both read the English announcements under `/eng/xw/zyxw/<yyyymm>/t<yyyymmdd>_<id>.html` | - | The authoritative source for the dates of a Chinese state visit, and it names the inviting head of state. Better than any news copy; find the URL with `WebSearch` since the index pages are not worth walking |
| **Fiscal policy outlets** (crfb.org, thecentersquare.com, federalnewsnetwork.com, epicforamerica.org, chds.us, nado.org, vcresearch.berkeley.edu) | All read fully with a browser UA; CRFB's `blogs/appropriations-watch-fy-<year>` is a live tally of the twelve bills, the votes and the current deadline | - | CRFB's Appropriations Watch is the best single `sourceUrl` for "where the budget stands"; agency and university shutdown guidance pages date a lapse from the inside |
| **congress.gov** | Nothing | **403 to `curl` and WebFetch alike** for `/crs-product/` pages | Cite the CRS report through `everycrsreport.com/reports/<id>.html`, which mirrors the same text and reads fine; confirm the mirror carries the sentence before relying on it |
| **ebu.ch** | WebFetch reads the full announcement | 403 to `curl` | Same shape as vaalit.fi: cite it on the strength of the WebFetch read |
| **Trade-show organiser sites** | For an annual show the **homepage is the current edition's page** - `gamescom.global/en` and `edfringe.com` carry the 2027 dates in plain HTML | `computextaipei.com.tw` renders its dates in JavaScript (a plain fetch shows none); `computex.biz` does not resolve | Grep the raw HTML for the year before using the homepage as `sourceUrl`; if it is not there, use the headless scrape or drop the candidate |
| **USGS earthquake catalogue** (`earthquake.usgs.gov`) | The **FDSN API** is the gold standard and keyless: `/fdsnws/event/1/query?format=geojson&starttime=&endtime=&minmagnitude=` returns the event id, magnitude, origin time in epoch ms, epicentre and depth for any historic quake | The human `/earthquakes/eventpage/<id>/executive` page answers **200 but renders as pure site chrome** through the headless browser - an Angular shell whose content never loads, so a scrape of it yields nav links and a footer | Take the facts from the API and use the eventpage only as a `sourceUrl` or reference. **Query by date window, not by guessed slug**: `usgs.gov/programs/earthquake-hazards/science/<event>` paths are not stable and 404. Beware the aftershock trap - a window of one UTC day around Tangshan returns the M7.4 Luanxian *aftershock*, not the M7.5 main shock, which sits in the previous UTC day |
| **Smithsonian Global Volcanism Program** (`volcano.si.edu`) | **The app's own headless scraper reads it** (`volcano.cfm?vn=<number>` gives the volcano's identity and eruption history) | **403 to `curl`** with a browser UA, so a session's own tools see nothing | Same shape as reuters.com: do not judge it by `curl`. Confirm a volcano number through `GET /api/scrape` and read the `<title>` (`Global Volcanism Program \| Tambora`) before citing it - the numbers are not guessable, and citing the wrong one silently attributes the eruption to another volcano |
| **NOAA NCEI HazEL** (`ngdc.noaa.gov/hazel/...`) | Nothing | Every path answers 200 with `We're sorry but HazEL doesn't work properly without JavaScript enabled` - a useless fetch that a status check passes | Do not cite it. For a historic tsunami or earthquake go to the USGS catalogue instead |
| **National weather and climate agencies** (`nhc.noaa.gov`, `noaa.gov`, `weather.gov`, `bom.gov.au`, `pagasa.dost.gov.ph`, `ipcc.ch`, `wmo.int`, `vedur.is`) | All read with a plain browser UA, including NHC's Tropical Cyclone Report **PDFs** (`/data/tcr/AL122005_Katrina.pdf`), which carry landfall times to the minute. `nhc.noaa.gov/climo/` states the official season bounds for the Atlantic and eastern Pacific; `bom.gov.au/climate/cyclones/australia/` states the Australian region's; `ipcc.ch/reports/` lists each forthcoming report with its month | `weather.gov` office paths are not guessable (`/hgx/hurricane_galveston1900` 404s while `/oun/events-19350414` is real); `wmo.int/en` and a guessed `/media/news/<slug>` 404, and `rfs.nsw.gov.au` is behind Cloudflare (403) | These are the authoritative sources for this vertical and none of them needs a key. Find a WMO news slug by `WebSearch` rather than guessing, and grep a fetched agency page for the sentence you intend to quote before citing it |
| **MICHELIN Guide** (`guide.michelin.com`) | **The app's own headless scraper reads it in full** - the inspectors' verdict, the price band (`$$$$`), "Good for" flags and a "N people love this place" count | **202 with an empty body to `curl`**, the same AWS WAF shape as IMDb. Worse, the rendered body contains the strings `Three Stars`, `Bib Gourmand` and `Green Star Community` as **navigation furniture**, so grepping the page text for a star count returns whichever filter link appears first - it read "Green Star Community" on a three-star restaurant and "Bib Gourmand" on a two-star one | Take the award from the **meta description**, which states it unambiguously ("a Three Stars: Exceptional cuisine restaurant in the 2026 MICHELIN Guide USA") and also carries the guide year. Do not grep the body. Restaurant URLs are `guide.michelin.com/<cc>/<lang>/<region>/<city>/restaurant/<slug>` but the region segment is not guessable - find it by search |
| **news3lv.com** (KSNV) and Sinclair stations | Plain `curl` with a browser UA. The page embeds a full `NewsArticle` **JSON-LD** block carrying the entire `articleBody`, the byline, `datePublished` and `dateModified`, and every image with its caption and dimensions - one fetch is enough for the whole story | The station's own clip is a **Sinclair mp4** (`harvest-media-clips.sinclairstoryline.com`), not YouTube, so it cannot be saved as a medium (`mediumID` has only image, twitter and youtube) | Parse the JSON-LD rather than the rendered paragraphs; the body text is cleaner there. For the video, look for the same story on a local newspaper's YouTube channel - the Las Vegas Review-Journal covered this one five days later |
| **staradvertiser.com** (Honolulu Star-Advertiser) | `curl` with a browser UA returns the page, and the **intro paragraphs plus the whole per-restaurant body** are in the markup | It is a **premium** paper: the article is cut off partway with a subscription prompt, and there is no JSON-LD `articleBody` to fall back on. The cut comes *after* several hundred usable words, so a length check will not notice | Read what is served and cite only what you actually read. Here the visible part carried the opening date, the restaurant count and four full stall write-ups - enough to reference without the rest. Photo credits are in the captions, which do survive the cut |
| **publico.pt** | Nothing reads it - `curl` returns **202 with an empty body**, WebFetch gets a 403, and the app's own headless scraper throws `No text found`. The first source in this corpus to beat all three where a *Portuguese* paper was the obvious authority | Its 2014 archive is where the Time Out Market's opening was previewed (14 and 17 May 2014), so the day looked unreachable | Find the date in a later piece that restates it. Notícias ao Minuto's fifth-anniversary report carried "abriu portas a 18 de maio de 2014" in its first line - **an anniversary story is a dating source**, and often a better one than the preview, because it is written after the fact |
| **gothamist.com** | Redirects to `tollbit.gothamist.com`, which reads in full through WebFetch (call the redirect URL) and through the app's scraper | The redirect is not followed automatically; the original URL just returns a 302 | Follow it and store the `tollbit.` URL as the link, which is what the app then re-reads cleanly. Its 2016 archive is intact and carries the opening dates of restaurants that have since changed their addresses |
| **timeout.com/about/latest-news** | Plain `curl` and WebFetch both read the corporate announcements in full, including the `og:image` render | These are the only place Time Out publishes an opening window for a new market, and they give a **quarter or a season**, never a day | Good for the announcement, the partner and the square footage; take the close of the stated period as the date. The consumer `timeout.com/time-out-market-<city>` page is undated and says only the year |
| **guide.michelin.com** (addition) | Confirmed again: `curl` gets an empty 202, the app's headless scraper reads the whole entry | The star is in the entry and in the Guide's own magazine copy ("newly awarded One-Star restaurant"), but the meta description was **not** retrievable here because `curl` never got a body | When the meta-description route is closed, read the captured text for the Guide's own article wording rather than grepping the body for "Star", which is still navigation furniture |
| **cravedfw.com, fsrmagazine.com, dallas.culturemap.com** | All three read through WebFetch and the app's scraper | Their sister outlets do not: **wfaa.com**, **papercitymag.com** and **dallasobserver.com** all defeat `curl`, WebFetch and the headless scraper alike | For a US local-restaurant story there are usually five or six outlets carrying the same press release; test the small ones first, they block least |
| **honolulumagazine.com** | Plain `curl`; the article body is ordinary markup | Its evergreen food pieces carry a dated **"Editor's note"** inserted long after publication ("By the middle of 2023, the number of eateries at Stix had expanded to 16"), which sits above the original text and silently contradicts the body's own figure | Read the whole page before quoting a count from it. The editor's note is usually the **newer** fact and the body is the day-of one; a pin can carry both if it says which moment each belongs to |
| **stixasia.com** and operator sites of this kind | Plain `curl` | The page is undated, gives no opening date beyond a year, and describes the venue **as it is now** - counts are marketing rounding ("over 15 authentic cuisines") that can disagree with contemporaneous press. Its cuisine list also differed from a newspaper's for the same hall (Singapore vs Vietnam) | Good for the address, the current format and the operator's own wording; never for a date, and prefer the day's press for any count. Where the operator and a secondary outlet disagree about the operator's own venue, the operator wins |
| **Academic publisher paywalls** (`dl.acm.org`, `journals.uchicago.edu`, `ieeexplore.ieee.org`) | Nothing | **403, 403 and 202-with-an-empty-body** to `curl`, to WebFetch *and* to the app's own headless scraper - the only sources met so far that defeat all three. A scrape of them returns the bare hostname as the title and no text | Go to the copy an author or a university hosts, which is usually better evidence anyway because it carries the **printed dateline**: Hellman's own `ee.stanford.edu/~hellman/publications/24.pdf` gives "IEEE TRANSACTIONS ON INFORMATION THEORY, VOL. IT-22, NO. 6, NOVEMBER 1976", `seas.upenn.edu/~zives/03f/cis550/codd.pdf` gives "Volume 13 / Number 6 / June, 1970", and the Princeton course copy of Black-Scholes carries the JSTOR cover sheet with the full citation. Note that a **two-column scan interleaves its columns** under `pdftotext`, so quote only contiguous fragments |
| **Commons oversized files** | `iiurlwidth=<px>` on the `imageinfo` query returns a `thumburl` at that width, and the thumbnail saves normally | A file over about 40 megapixels **fails the save outright**: the ESO re-processing of the 1919 eclipse plate is 23800x14191 and the pin came back "Picture is too large to decode (maxResolutionInMP limit exceeded by 38MP)". Worse, `POST /api/pins` is not transactional, so the pin is created without its media or its tags | Check `width * height` before posting and ask for `iiurlwidth=1920` on anything large, taking the `thumburl` (split on `?` as ever). If a save has already failed this way, **`PUT` the half-saved pin** rather than posting a second one |
| **einsteinpapers.press.princeton.edu** | Nothing any more | `/vol6-trans/129` answers **200 with a portal advertisement** - "Einstein Portal: Launching September 30th" - where it used to serve the paper. A status check passes and a byte count says nothing | The live text of Einstein's 1915 field-equations paper is on **Wikisource** (`Translation:The_Field_Equations_of_Gravitation`), whose header also carries the Sitzungsberichte citation and the session date. General rule: a publisher that has moved to a database landing page leaves every deep link answering 200 - grep the fetched text for a sentence you expect before citing it |
| **Nominatim** (`nominatim.openstreetmap.org`) | The only geocoder this project has - the app ships none. Forward geocoding an address often resolves to the **business itself** (`type: restaurant`, with its name in `display_name`), which both confirms the address and gives a precise point; reverse geocoding a coordinate returns a real address label, and `Accept-Language: en` transliterates it | Rate-limited, so space requests about 1.5s apart; a street number may resolve to an interpolated point rather than the premises | Send a User-Agent naming the client and a contact - a generic contact, never the owner's email (the same goes for sec.gov's required contact header). Parallel agents get 429 for a whole run; stagger them, or fall back to Photon (`photon.komoot.io`, the same OSM data) for forward and reverse. **Reverse-geocode the coordinates to get the address label rather than typing a street address**: on one batch of six restaurants, three hand-written addresses were wrong and reverse geocoding caught all three (The French Laundry's coordinate is on Creek Street, not Washington Street; Gaggan is on Sarasin Road, not Soi Langsuan; Mugaritz's coordinate falls in Astigarraga though the Guide files it under Errenteria) |
| **Photon** (`photon.komoot.io`) | Keyless forward (`/api?q=`) and reverse (`/reverse?lat=&lon=`) geocoding on OSM data; finds named OSM features ("Microsoft Fairwater AI Datacenter") | - | The fallback when Nominatim answers 429, which it did to every request with four agents sharing an IP (2026-09-22); the label is still OSM's own, never typed |
| **Yelp, Tripadvisor** | Nothing without a key. Yelp Fusion with `YELP_API_KEY` gives the rating, review count and a booking link for businesses whose `transactions` include `restaurant_reservation` | **403 to `curl`** on every business and review path tried; keyless the API answers `VALIDATION_ERROR: Authorization is a required parameter`. Yelp thins out fast outside the US, so Osteria Francescana and Sukiyabashi Jiro are Google-only | There is no keyless route to their ratings - **not even through our own browser, which Yelp 403s too**, so unlike Google there is no scrape fallback. Free-tier key in `.env.local`; the score is **fetched on view and never stored** (`src/server/places.ts`) because Yelp's terms cap how long it may be kept. For a rating that *is* storable use the MICHELIN Guide; a star count is a real score, a Guide selection with no star is not and belongs in a tag |
| **therundown.ai** | Plain `curl` with a browser UA reads the whole article, and its **"Sources & further reading" block deep-links every original it used** | It is a newsletter write-up that rewrites other outlets - its own footer says "This story builds on reporting from The Rundown newsletter" - so under the reference rule it is an aggregator | Read the body links and follow the credit chain one step further than it goes - TechCrunch credited a Reuters exclusive published thirteen hours earlier - and put the outlets it names at the **top of the reference list**. It is a fine `sourceUrl` when that is the page you were given (owner, 2026-09-22), and a poor reference on its own: everything in it is attributed to somebody else |
| **techcrunch.com** | Plain `curl` with a browser UA; `article:published_time` and the byline's "4:13 PM PDT · September 18, 2026" both date the story, and body links carry the primary sources and the outlet it is following | A regex for the `entry-content` div **truncates the article to its first two paragraphs** - the wrapper closes earlier than the greedy match expects | Extract paragraph by paragraph (`<p>...</p>`) rather than by container, and keep each paragraph's `href`s with it: that is where the Reuters scoop, the Novo Nordisk release and the company's own announcements were |
| **Google Places API (New)** (`places.googleapis.com`) | With `GOOGLE_PLACES_API_KEY`: rating, `userRatingCount`, `currentOpeningHours` and a `googleMapsUri`. `places:searchText` resolves a name plus a coordinate bias to a place id | Keyless it is `PERMISSION_DENIED`. **Billed per request and per field group** - `rating` is Pro and `currentOpeningHours` is Enterprise, so the field mask is the bill; `reviews` is deliberately not asked for. It has **no popular-times, busyness or wait-time field**; Google documents those as a Maps and Search display feature | Keep the field mask (`GOOGLE_FIELDS` in `src/server/places.ts`) as short as the panel needs. The place id may be cached indefinitely and is the only part stored; the scores are fetched on view |
| **Google Maps place page** (rating, keyless) | **Works through the app's own browser**, like the MICHELIN Guide: the place panel gives the name, the rating as a `4.6 stars` aria-label, the opening state ("Closes soon · 8 PM · Opens 4 PM Tue") and *sometimes* the rating count | **The panel renders reduced about half the time** - nav chips and nothing else - if you wait on `networkidle2` plus a delay. The **rating count is intermittent**: the same place minutes apart gave "4.6 (2,275)" and then no count at all. Taking "the first bracketed number" reads the phone number's **area code** as a count (707 ratings for The French Laundry) | **Wait for `[aria-label*="stars"]`**, do not sleep - that took it from flaky to 4 reads out of 4. Take the rating from the aria-label, and match the count **as a pair with the rating**. COALESCE the count so an absent one never wipes a stored one. `src/server/placeScrape.ts`, `npm run places:refresh` |
| **Google Maps place page** (busyness) | **Nothing reachable from a datacenter IP.** A plain fetch gives map-tile state with the place name absent; `/search?tbm=map&tch=1` with the page's own `pb` gives the name and place id but no busyness; `google.com/search` gives nothing without JS; headless Chromium loads the panel (name, rating, review count) but **no Popular times section**, and a second load dropped the review count as well | Scraping it breaks Google's terms, and the markup is Google's to change without notice. The reduced panel on a repeat load looks like the UI Google serves a client it distrusts | `/search?tbm=map` **does** hand out a place id keylessly (`ChIJCar0f49ZwokR6ozLV-dHNTE` for Katz's), which is the cheap half of resolving a place. The busyness read lives in `src/server/googleBusyness.ts` behind a circuit breaker so a silent Google costs nothing but the busy bar. A paid feed (BestTime.app, SafeGraph) is the alternative if the number matters |
| **Reservation sites** (OpenTable, Resy, Tock) | Taking the booking link from the **restaurant's own site** - `thomaskeller.com/tfl` links out to `exploretock.com/tfl/` | **OpenTable refuses `curl` outright** (connection failure, not a status). **Resy answers 200 for a venue path and serves an SPA shell** whose `og:title` is the generic "Right This Way", so a 200 is no evidence the venue is the right one. None has a public API | Find the link, never construct it - the merchant-link rule. A hand-added link is stored `resolvedBy = 'hand'` and a later `places:resolve` run will not overwrite it |
| **Local restaurant press** (`sandiegoville.com`, `whatnow.com`, `sandiego.eater.com`) | All read with a plain browser UA. Eater and Whatnow publish `article:published_time`; Whatnow's body also states the expected opening window in prose ("expected to open later in 2026") | **SanDiegoVille is Blogger and publishes no `article:published_time`** - its date is only in the visible `date-header` markup, and the `/YYYY/MM/` path gives the month but not the day | These are where a restaurant opening gets its date, since a group's own site rarely dates anything. For SanDiegoVille, grep the rendered text for a `Month D, YYYY` string rather than trusting metadata |
| **Restaurant group sites** (`chubbygroup.com`) | The locations directory lists every brand and site, including a `(Coming Soon)` marker, and reads with a plain UA | **No dates anywhere** - roughly 60 locations across 9 paginated pages, and "Coming Soon" is the only forward signal | Good for confirming a brand belongs to a group and for the company reference, useless as a `sourceUrl` for an opening. Only the press-covered openings are pinnable |
| **Wikidata** | Keyless API for HQ (`P159`), review scores, episode counts (`P1113`, finished when `P582` is set), entity labels | - | First choice for studio HQ and IMDb/RT/Metacritic scores |
| **IMDb** | Nothing: an AWS WAF challenge (HTTP 202 empty body, 403 to fetch tools) blocks curl and WebFetch alike | Every request | Do not retry. Use `WebSearch "<title> imdb"` for the real `imdb.com/title/tt.../` URL as `sourceUrl` (a URL found by search but never fetched is still valid) and Wikipedia for facts and the poster. An `imdb.com/news/ni.../` item is a **syndicated copy** of another outlet's article, blocked the same way: `WebSearch` the bare news URL to get its title, search the title to find the publisher's original (CBR, Deadline, Variety, ...), and cite **that** - a blocked link can only ever hold a dead wiki |
| **MyAnimeList** | Page text through the browser; the app's scrape | Jikan (`api.jikan.moe`) 504s constantly | Prefer AniList by MAL id; retry Jikan with backoff; a page for an unaired title lists dates as "Not available" |
| **AniList** | GraphQL, keyless | Blocks python-urllib (use curl); one missing `idMal` nulls a whole batched query; Cloudflare 1015 rate limit under load; 429s within minutes when a backfill runs several workers at once, and then answers nothing for every pin | Query one id at a time, slowly |
| **YouTube** | oEmbed (validates embedding), Data API with `YOUTUBE_API_KEY`, page text | Caption downloads answer **429** from an address that fetched many at once, even from a real browser (2026-09-19) | Throttle (`npm run wiki:transcripts`, 4s apart, backs off), retry after a long cooldown; the player call still works, so a video's tracks can be listed. Without a transcript a video's wiki is description-only |
| **X / Twitter** | oEmbed for the post text; links written out | The linked pages may be blocked | Use the linked article as the reference |
| **Apple Podcasts / iTunes** | Search API keyless; episode page show notes | Apple's own transcripts need an Apple ID (do not lift tokens) | Transcript = the RSS `podcast:transcript`, else the show's full YouTube upload read through the caption reader |
| **Kalshi** | `events/{TICKER}?with_nested_markets=true` keyless; catalogue at `series?category=` (`Economics`, `Crypto`, `Science and Technology`, `Companies`, `Financials`, `World`, `Climate and Weather` - plain `Technology`/`Science` return null) | Per-title series (429s); empty top-level `markets` on some events; the **`events?status=open` list returns every price as `null`**; thin ladders quote out of order; kalshi.com itself 429s to curl | Use the KXRT/KXMC series; strike date is the decision time; scan by series then re-read each event for `*_dollars` prices; verify a web link with `parseMarketUrl` + `oddsFor`, not by fetching the page |
| **Ticketmaster** | The `discover.` announcement subdomain (plain `curl`, `og:image` is the tour press shot); the **artist** page's raw HTML, whose `schema.org` `Offer` per show carries the event URL, venue and local start time | The per-event page `/<slug>/event/<id>` - `GET /api/scrape` returns nothing but `{"type":"web"}`, and `curl` gets the SPA shell | Read the dates off the artist page; use the per-event URL as `sourceUrl` without fetching it (the IMDb rule) |
| **Polymarket** | Gamma `events?slug=` keyless | Empty-subscription firehose on the sockets | Market page as `sourceUrl`; a companion market is a reference |
| **GSMArena, SamMobile** | Plain `curl` with a browser UA, and the app's scraper; `og:image` is the article's own lead photo | - | Good for a vendor date a global press release omits - but say whose newsroom it came from: a Samsung Thailand post is one country's schedule until another portal repeats it |
| **Cloudflare / bot-check sites** (Bloomberg, GameSpot, Kickstarter, Neowin, TechPowerUp, eWeek, Britannica, Axios, dpreview, and many trade sites) | Sometimes with the plain-Chrome user agent | "Just a moment...", "Sorry, you have been blocked", robot checks, 403 | The fetched text is worthless: do not write a wiki from it. Try the site's RSS or an archived copy, else record the source as blocked and rely on another reference |
| **Windows Central, MSN and similar aggregators** | - | Return the current front page, not the old article | Treat as blocked (the page is not the article the URL names) 2026-09-22: a current Windows Central article (the 26H2 FAQ) read in full with plain `curl`, so the trap is old URLs, not the site |
| **openai.com, help.openai.com** | help.openai.com with the plain Chrome UA; the community forum (Discourse) for launch announcements | openai.com answers 403 to curl and WebFetch | Use the forum announcement and the help-centre release notes |
| **Microsoft's own sites** (blogs.microsoft.com, news.microsoft.com, news.xbox.com, xbox.com, techcommunity.microsoft.com, learn.microsoft.com, microsoft.com/en-us/investor, ignite.microsoft.com) | Plain `curl` with a browser UA reads them all; xbox.com store pages give edition prices, Xbox Wire's "gone gold" posts give a launch's exact UTC time, the IR home page's "next earnings release" panel says when a date is set | The app's scraper returned nothing for the Microsoft 365 blog, which curl read in full | Use Microsoft's own post as `sourceUrl` - it publishes even its layoff memos |
| **MSRC** (`msrc.microsoft.com`) | The keyless **CVRF API** answers `curl` with JSON: `api.msrc.microsoft.com/cvrf/v3.0/updates('<yyyy-Mon>')` lists a month's release, `/cvrf/<yyyy-Mon>` is the document (17 MB) with `Exploited:Yes` threats per CVE | The Security Update Guide and release-notes pages are an empty Angular shell to curl and the scraper | Date and name zero-days from the CVRF; too big to cite, so cite Microsoft Learn's release-cycle page and the press |
| **news.bloomberglaw.com** | Plain `curl` reads Bloomberg News exclusives in full, same byline and timestamp | bloomberg.com is 403 to curl and paywalled to the scraper | Use the Bloomberg Law copy as the readable source for a Bloomberg exclusive |
| **Steam store pages** | The API's `release_date` | The pipeline stores the **age gate** (a birth-date form), so a wiki can say nothing; its release time disagreed with Xbox's own by an hour | Never cite a Steam page; take launch times from the publisher |
| **Tokyo Game Show** | `tgs.cesa.or.jp/<year>/en` is the current official site | tgs.nikkeibp.co.jp and expo.nikkeibp.co.jp/tgs redirect to 2024 or 404 | Use the CESA site |
| **gameranx.com, IGN news listings** | Server-rendered: extract links with `grep -o` on the raw HTML; IGN body from the `<article>` block | WebFetch cannot reach ign.com; WebFetch's markdown drops `<iframe>` embeds | `curl -A "<browser UA>"`, then grep raw HTML for `<iframe ... youtube ...>` |
| **sandag.org, and agency sites like it** | The real path, found by grepping the homepage for `href="[^"]*<slug>"` | A guessed `/projects/<slug>` returns a 404 body at HTTP 200, and the body is nav, not content | Prefer Wikipedia for the project's history and the agency page only for today's promise |
| **gonctd.com, san.org, health.ucsd.edu, plandesignbuild.ucsd.edu** | WebFetch reads all four | UCSD's Plan Design Build page is accordions (`href="#"`), so it has no per-project deep link | Use the owner's own project page as `sourceUrl`; for UCSD that is health.ucsd.edu, not the listing |
| **cbs8.com** | - | 403 to WebFetch | Use the outlet the story came from, or Wikipedia |
| **californiaconstructionnews.com** | Plain WebFetch | Its og:image is the site logo | The article's own photo is under `wp-content/uploads/<year>/<month>/` in the raw HTML |
| **Transit and airport authorities** (mta.info, atl.com, flydenver.com, flysfo.com, changiairport.com, naa.jp, chicago.gov, engineering.lacity.gov) | The matching press release on a government or operator mirror - governor.ny.gov carries MTA's releases verbatim | 403 (Akamai/Cloudflare), or a nav-only body with the dates on another page | Take the date from the mirror, or from Wikipedia where the slip history lives |
| **News paywalls** (japantimes.co.jp 402, lasvegassun.com 402, reviewjournal.com, cbc.ca, torontolife.com, theengineer.co.uk, railjournal.com, seatrade-cruise.com, dailyherald.com) | Headline and lede only | The body | Find the same story at an outlet that answers, or drop the candidate |
| **cosm.com, and Next.js sites like it** | Nothing useful from the HTML | The reader captures the headline and stops - the article body lives in `__NEXT_DATA__` as Markdown content blocks, so a source comes back ~90 characters long | Check for a `__NEXT_DATA__` script before believing a short capture; a 90-character "article" is a rendering problem, not a thin page |
| **wpcentral.com** | Nothing - the domain is retired | Every URL redirects to the Windows Central front page | Dead as a source. 15 stored links were byte-identical homepage captures; they are marked blocked. Use a web archive if a pin really needs one |
| **clinicaltrials.gov** | The **API v2**, which the drug-readout vertical already uses | The study page renders its record in JavaScript, so a fetch captures the glossary and nav shell - identical for every study | Never wiki a study from its web page; 23 stored links were the same boilerplate and are marked blocked |
| **PDFs** | Read by the pipeline since 2026-09-20 ([pdfText.ts](../../../src/server/scrape/pdfText.ts)): `pdftotext` for the text layer, `pdftoppm` + `tesseract` OCR when there is none | A PDF over 40MB, or one that is a scan beyond its first 5 pages | Nothing by hand. Needs poppler and tesseract on the host (`brew install poppler tesseract`); the reader names the missing binary rather than failing obscurely |
| **Robotics makers' newsrooms** (figure.ai, bostondynamics.com, agibot.com, 1x.tech, investor.nvidia.com, aboutamazon.com, globenewswire, scmp.com, robotstart.info) | All read fully by `GET /api/scrape`, with dated bodies and Open Graph media | agibot.com's own article image is missing from the rendered image list; automate.org 403s to WebFetch though the app's scraper reads it | Take the date from the page, not a search summary; where the article's picture is absent, use a reference article's lead image |
| **Contentful image CDN** (`images.ctfassets.net`, used by figure.ai) | The bare asset URL | A `?fm=webp` variant fails the thumbnailer with "Mime type image/webp does not support decoding", 500ing `POST /api/pins` after the pin row is written | Strip the query string before saving, and `PUT` the whole pin back to repair one that was created without its media |
| **Aerospace manufacturers and agencies** (boeing.mediaroom.com, airbus.com, nasa.gov, aerospacetestinginternational.com, aircargonews.net, militarytimes.com, airandspaceforces.com, rafmuseum.org.uk, airandspace.si.edu, aerospacebristol.org, thisdayinaviation.com, aerotime.aero) | All read fully by plain `curl` with a browser UA and by `GET /api/scrape`; NASA and Boeing release pages carry dated bodies and large Open Graph photos | `baesystems.com` is behind **Incapsula** - every heritage page is 923 bytes of iframe shell; `museumofflight.org` refuses the connection outright; `aviationa2z.com` began answering **522** mid-run | Use the manufacturer's or agency's own release where there is one. For BAE heritage subjects go to the RAF Museum or Aerospace Bristol instead; the Smithsonian's collection-object pages are the best record for an airframe it holds |
| **siae.fr** (Paris Air Show) | Loads, and the dates are in the markup | The whole page is 393 characters - a date banner and a cookie notice, no prose | Enough to date the pin and nothing else: take the organiser, venue history and scale from the Wikipedia article as a reference |
| **`.jpg.webp` image CDNs** (`s44864.pcdn.co` and other WordPress/PCDN hosts) | Dropping the trailing `.webp` returns the underlying `image/jpeg`, which the thumbnailer takes | The `.webp` itself fails the thumbnailer, like the Contentful case above | Strip the suffix. Where the file is *natively* `.webp` (`aviationa2z.com/.../Airbus-A350F.webp`) there is nothing underneath and the picture is unavailable |
| **nytimes.com interactives** (The Restaurant List 2026) | Nothing through `curl`/WebFetch - paywall | Everything, when read in the owner's logged-in Chrome through Claude in Chrome | Each entry is a `div id="<slug>"`, so `#<slug>` is the per-item `sourceUrl`; the header line gives city, cuisine, opening month and site. `get_page_text` returns the full list; JS results truncate near 1,000 characters |
| **Squarespace image CDN** (`images.squarespace-cdn.com`, `static1.squarespace.com`) | JPEG when `Accept` asks for it | WebP for `Accept: */*`, whatever `?format=` says - the thumbnailer rejects it | Fixed in `src/server/image.ts` (the downloader now sends an `Accept` header); a URL ending `.webp` on any host is still unusable |
| **WordPress restaurant sites behind a WAF** (littlebirdfairhope.com) | Pages and images with a browser UA | Images 403 to the app's `compatible; Chronopin/1.0` UA | Pre-check chosen images with the app's own headers; post without the picture if they fail |
| **Paywalled pages** | The teaser (headline and lede) | The body | A short wiki from the visible opening is acceptable; do not invent |
| **Shopify storefront blogs** (`ridealso.com/blogs/all/<slug>`) | **The app's own headless scraper reads the post in full**, including the visible date line (`08-19-2026`) the body carries instead of metadata | **Plain `curl` gets the theme shell only** - about 340KB of nav, cart and footer with no article text, no `datePublished`, no `article:published_time` and no JSON-LD `Article`. The blog *index* does carry the post links, so a `curl` of `/blogs/all` is still the cheap way to enumerate entries | Enumerate the posts with `curl` on the index, then read each one with `GET /api/scrape`. Do not conclude a post is undated because the markup has no date: these themes print the date in the rendered body, where only the browser sees it |
| **businesswire.com** | Nothing | **403 to WebFetch** on `secure.businesswire.com` and `www.businesswire.com` alike | The wire mirrors the release in full on Yahoo Finance (`finance.yahoo.com/.../<slug>-<id>.html`), dateline and quotes intact; find that URL by `WebSearch` and cite it as the official announcement |
| **jnj.com** press releases | **The app's own `GET /api/scrape`** reads each release in full | `curl` gets a 556-byte "Site Maintenance" page. A slug without a year can resolve to an **older release of the same title** - `.../johnson-johnson-to-host-investor-conference-call-on-third-quarter-results` returned the Q3 **2014** notice | Cite the jnj.com release as `sourceUrl` after checking the dateline year. The release time is in the SEC 8-K Exhibit 99.1 copy ("FOR RELEASE AT 6:15PM ET") or in PR Newswire's `<meta name='date'>`, never on jnj.com. Pharma releases are datelined at the unit (Spring House, PA for immunology, Titusville, NJ for neuroscience), corporate ones at New Brunswick |
| **investor.jnj.com** | Its **Q4 feed API answers `curl` with JSON**: `/feed/PressRelease.svc/GetPressReleaseList?LanguageId=1&pageSize=60&year=2026`, `/feed/Event.svc/GetEventList`, with every attachment (call transcripts, slide decks, the pre-call "Economic Factors" note) on `s203.q4cdn.com`, which `curl` and the PDF reader both take | The news-details pages 403 to `curl` (the app's scraper reads them) | The best primary source for a J&J date or quote: an investor-day date said on an earnings call is in the call transcript before any press release carries it |
| **NVIDIA** (nvidianews.nvidia.com, blogs.nvidia.com, nvidia.com, investor.nvidia.com) | The newsroom list `?page=N`, `rss.xml` (every item's GMT time), blog posts (`article:published_time`), nvidia.com/gtc pages and FAQs, `forums.developer.nvidia.com/t/<slug>.json` - all plain `curl` with a browser UA; investor.nvidia.com's **Q4 feed API** (`/feed/FinancialReport.svc/GetFinancialReportList`, `/feed/Event.svc/GetEventList`) and q4cdn PDFs | investor.nvidia.com's HTML pages 403 to `curl` (the app's scraper reads them); release pages show a date without a time and carry no dateline; `og:image` is often a logo lockup; GTC sub-pages can still show last year's edition | Time an item from the RSS or the blog's published time, find the venue in the trade press, and read the next earnings day from the end of the last call transcript |
| **hpe.com** (newsroom, Discover pages) | **The app's own `GET /api/scrape`** reads each release and event page, in about a minute | **`curl` hangs** (status 000 after 30s, an Akamai tarpit), and WebFetch timed out at 60s on a release. The scrape's picked media were wrong on every page tried - a Tottenham kit, an **HP Inc** LaserJet (a different company), AMD chips from Wikipedia, sponsor logos from Rainfocus. A psnow PDF (`/psnow/doc/a00162088enw`) comes back as site chrome only. An event page is **rolled over to the next edition** - `discover/lasvegas.html` read "June 21-24, 2027" in September 2026 | Read through the scraper and pick every picture by hand. For a PDF, cite the resource-library page of the same document number (`/uk/en/resources.guide....a00162088enw.html`). A past edition's source is its on-demand page, not the rolling event page |
| **investors.hpe.com** | Plain `curl` with a Chrome UA. `/news-and-events` lists coming events with exact PT times; `/financial/quarterly-results` links every call transcript PDF, each opening with the call's date and time; conference appearances (Citi, Goldman Sachs) have transcripts too, which preview coming events | Transcript file names are irregular | Take the PDF links from the results page, never guess them. HPE posts no earnings-call video to YouTube (its last 80 uploads checked); third-party uploads of the call (Benzinga, Castify) are not the company's |
| **olcf.ornl.gov** | The system page and photo attachment pages read with `curl` | The system page shows photos with alt text but **no dates** | The WordPress media API (`/wp-json/wp/v2/media?search=<file>`) gives each photo's dated caption - it was the only source dating Lux's delivery (17 June 2026) |
| **CourtListener RECAP** | `storage.courtlistener.com/recap/.../<case>.<doc>.0.pdf` reads; a later docket entry is the same URL with the document number changed | Only entries someone has bought through RECAP exist | A trade article's link to one entry opens the rest of the docket; a state AG party often hosts its own copy of the order (oag.ca.gov) |
| **TSMC's own sites** (tsmc.com, pr.tsmc.com, investor.tsmc.com, esmc.eu) | **The app's own `GET /api/scrape`** reads every release, technology, event and IR page; WebFetch reads them too and keeps the link URLs, and saves the IR PDFs (call transcripts) for `pdftotext` | **Cloudflare 403 to `curl`** on every page, PDF and image, so tsmc.com pictures cannot be media; the scrape's picture top-up offers unrelated Wikipedia photos (Apple's A16 Bionic for the A16 node, a GTX 1070 on the ESMC release); the annual report's shareholder letter loads in a tab the scraper misses; `wiki:export` cannot fetch the IR PDFs | Take text from the scrape or WebFetch, pictures from cited coverage or Commons, and the official copy of any release from EDGAR 6-Ks (CIK 1046179, UA naming `admin@chronopin.app`). TSMC's YouTube channel is **@tsmc4361** (`UC02yNxGj2MxhynehcWSxcLg`); @tsmc and @tsmcofficial are other people |
| **focustaiwan.tw, cna.com.tw** (Central News Agency) | Plain `curl` with a browser UA: `article:published_time`, full-size photos (`WebCover/800` in the JSON-LD) and embedded CNA YouTube videos | Focus Taiwan articles older than about 6 months sit behind the CNA archive paywall; CNA often leads with a generic TSMC logo photo | The Chinese CNA copy stays open; the Taipei Times and GlobalSecurity.org carry the full CNA English text |
| **Taiwan prosecutors and courts** (`thip.moj.gov.tw`, `*.judicial.gov.tw`) | The prosecutors' bulletin list `thip.moj.gov.tw/12606/914012/12690/Normalnodelist` reads with `curl`; each release gives an ROC-calendar 發布日期 and "今（N）日", which dates the step exactly | **Every judicial.gov.tw court site answers `curl` with a CAPTCHA** | Take rulings from CNA / Focus Taiwan and the Supreme Court spokesman as quoted by PTS |
| **commerce.gov** | Nothing | 403 to `curl` and WebFetch, a Cloudflare page to the app's scraper | Use the USTR fact sheet (undated in its text; the URL path carries the month), the Reuters copy on Yahoo Finance, or a law-firm summary |
| **Bloomberg through Yahoo Finance** | `finance.yahoo.com/.../<slug>-<id>.html` carries Bloomberg's full syndicated article with `datePublished`, readable by `curl` | bloomberg.com is a bot wall | Cite the Yahoo copy for a Bloomberg scoop, and date the pin to Bloomberg's own day - the follow-ups (Quartz, trade press) run days later (the Apollo/DePuy scoop was 11 September; the brief had 14th). Reuters wire copy likewise reads on local radio sites (kelo.com) |
| **babcock.com** (Babcock & Wilcox newsroom) | Plain `curl` with a browser UA reads every release (`/home/about/corporate/news/<slug>`, list pages `?start=0,12,24,36`) with a "Posted" date and dateline, and the product pages' `/assets/` images download through the app | Releases carry **no time**, and nearly every `og:image` is `Babcock-Wilcox-logo.jpg`. Pairing list-page links with the nearest date on the page gives neighbours' dates | Date from the release itself; time it from the Business Wire copy on Yahoo or the 8-K's EDGAR acceptance stamp. Take pictures from the card image on B&W's topic/product pages (strip the `__Fill...`/`__ResizedImage...` suffix for the original) and from `investors.babcock.com`'s investor-deck slides (3000px; only the current deck) |
| **Business Wire copies on Yahoo Finance** (`finance.yahoo.com/news/<slug>-<digits>.html`) | `datePublished` with the exact time, and the URL's numeric suffix encodes the UTC time (`-103000775` = 10:30:00Z) | The `og:image` is the issuer's logo; some older copies 404 | Use it for a release's time when businesswire.com itself is blocked; Morningstar's copies carry wrong offsets |
| **TownNews papers** (bismarcktribune.com and other `bloximages` sites) | The page with byline and date | Article paragraphs are **ROT47-encoded** in the raw HTML (lines starting `kAm`), and the app stores that encoded text as the source text | Decode ROT47 before reading or writing the wiki |
| **sableoffshore.com** (Sable Offshore newsroom) | The app's headless scraper (`GET/POST /api/scrape`) reads each release | **403 to `curl`** with a browser UA; release `og:image`s are logos | Scrape through the app; take the filed copy from EDGAR (CIK 1831481, 8-K Exhibit 99.1) for citations and the 8-K acceptance time for the hour |

**anthropic.com** - reads in full to plain `curl` with a desktop Chrome user
agent; no challenge. The announcement page carries **no publication date**: it
says "September 2026" and "available today", and its only ISO stamp is a build
time. The date is on the card in `anthropic.com/news`, read from that entry's
own `<a>` element rather than from the flattened text, where the dates sit
ambiguously between titles (each card is a self-contained anchor holding its
own `<time>`; only the featured card puts the title first). **Cite the index as
well as the article** - the article cannot back its own date. Images are `cdn.sanity.io/images/4zrzovbb/website/`
URLs with the dimensions in the filename; the `og:image` and at least one
in-page asset are the **wordmark**, so open them before keeping them.

**venturebeat.com** - a Vercel Security Checkpoint: 429 with ~280 characters of
challenge to `curl`. Not citable without reading it another way.

**SEC EDGAR** - keyless with a descriptive User-Agent. `sec.gov/files/company_tickers.json`
gives the CIK, `data.sec.gov/submissions/CIK##########.json` lists every filing
with its form, items and accession number, so a restructuring 8-K (Item 2.05)
or a foreign issuer's 6-K is found without a web search. The 8-K states the
share of staff and the charge; the next 10-Q's restructuring note gives any
addition to it; the cover page gives the HQ address to geocode. It settles the
dates and figures that layoff trackers get wrong.

**State WARN notices** - Tennessee's reports page
(`tn.gov/workforce/general-resources/major-publications0/major-publications-redirect/reports.html`)
links one PDF memo per notice with the street address, headcount and effective
date, readable with `curl` + `pdftotext`. New Jersey publishes one yearly PDF
(`nj.gov/labor/assets/PDFs/WARN/2026_WARN_Notice_Archive.pdf`) - a single URL,
so only one pin can take it as `sourceUrl` and the rest cite it as a reference.
Washington's WARN database is client-rendered and unreadable to `curl`; GeekWire
reports its counts.

**YouTube search without the quota** - `curl` of
`youtube.com/results?search_query=...` (add `&sp=EgIIBQ%253D%253D` for this
year) and parsing `ytInitialData` for `videoRenderer` entries finds news
reports without spending WebSearch; verify the channel with oEmbed.

**Also blocked to `curl` (2026-09-22):** timesofisrael.com (Cloudflare
captcha), king5.com (Akamai Access Denied), qz.com and inman.com (empty page),
Euronews, Forbes, Washington Times, and `media.jlr.com` (client-rendered - quote
JLR's statement through CNBC). investing.com transcripts block `curl` but read
through WebFetch.

**macrumors.com** - reads in full and carries a real
`article:published_time`, which makes it a usable same-day date witness for a
vendor announcement that does not date itself.

# PDFs

A filing, docket, environmental statement or regulator letter is often the only
source a pin has, and until 2026-09-20 the reader refused them outright - the
fetch stage is the one stage that may fail a whole scrape, so a PDF link killed
the job. [pdfText.ts](../../../src/server/scrape/pdfText.ts) now reads them.

* **The text layer first, OCR only as a last resort.** `pdftotext` is
  milliseconds; OCR is about five seconds a page. It runs only when the text
  layer is under 200 characters - which is a header and a page number, not a
  document - and only over the first 5 pages.
* **Plain `pdftotext`, never `-layout`.** `-layout` preserves the geometry,
  which is right for a table and wrong for everything else: on a three-column
  Federal Register notice it interleaves the columns line by line, so every
  sentence reads as three unrelated half-sentences. Plain mode does the
  reading-order analysis and returns column after column.
* **Trust the bytes, not the content type.** A PDF is regularly served as
  `application/octet-stream`, and a block page is occasionally served as
  `application/pdf`. Anything that is not a web page has its first bytes
  checked for `%PDF`.
* The text is prefixed with what was read - `[PDF, 340 pages; text of the
  first 50]`, `[Scanned PDF, 12 page(s); OCR of the first 5]` - so a summary
  written from it is not taken for the whole document.
* Caps: 40MB, 50 pages of text, 5 pages of OCR at 300 dpi.

# Recognising a useless fetch

`looksBlocked(text)` in [sourceText.ts](../../../src/server/scrape/sourceText.ts) flags a short page (under 1,500 characters) that says "Just a moment", "Attention Required", "Access Denied", "You have been blocked", "Are you a robot", "403 Forbidden", "Too Many Requests", "page not found" and the like. The reader no longer accepts such text: it loads the page in the headless browser as plain Chrome (the `HeadlessChrome` user agent is what Cloudflare holds), waits up to 12 seconds for a JavaScript challenge to clear, and fails the link with `Blocked: ...` if it is still a block page, so no wiki is written from it. `npm run wiki:refetch-blocked` reads the already-stored blocked and failed links again this way (in the first check, 3 of 5 read).

Treat the text as **not the article** (record it as blocked, do not summarise it) when it is: a Cloudflare, Akamai or CloudFront challenge; a 403, 404 or 400 page; a login or age gate; a site homepage, index or listing where the URL names an article; a cookie or navigation shell with no facts about the pin's event; or the page for a different event that shares a name (a disambiguation page, a redirect to a broader article).

# A live URL that serves a different article

Worse than a block page, because nothing flags it: the host kept the URL,
answers 200 and serves a real, well-formed article - a *different* one.
`computerworld.com/article/3155017/dell-to-ship-first-32-inch-8k-display-...`
redirects to "Dell Wyse 5070 vs the Always Connected PC" and stored 11,435
characters of it, so `looksBlocked` passed it and a reference cited a headline
the link can no longer show. Neither a status check nor a length check sees
this. What sees it is the title:

```sql
SELECT "r"."pinId", "r"."title", "s"."title"
FROM "PinReference" AS "r" INNER JOIN "Source" AS "s" ON "s"."url" = "r"."url"
WHERE lower(left("s"."title", 25)) <> lower(left("r"."title", 25));
```

The same check reads a reference's own `reasoning` as a confession: a
reasoning that says *could not be fetched*, *returned unrelated content*,
*its own headline* or *a year after the pin's stored date* is a reference that
was written without reading the page, and every one found this way was wrong.

# A dated figure is the year check

An index or a report headlined by month alone repeats every year, and the
archive keeps them all. Hunting an April 2023 Manheim story turned up
`press.manheim.com/Manheim-Reports-Used-Vehicle-Value-Index-Results-for-April`
(index 124.2) and Vehicle Remarketing's "Manheim Used Vehicle Index Hits
Three-Year Low in April" (125.8), neither of them 2023, whose reading is 230.8.
Match the **value**, not the month, before citing a series like this one.
`vehicleremarket.com/10198264/...` is the April 2023 piece and reads in full.

# WebFetch is not a source

WebFetch's summary is a paraphrase, and it has been caught inventing a figure that appears nowhere in the page (a cost for Shanghai Metro Line 19) and mangling a Unicode filename so the image URL 404'd. Quote numbers, dates and file names from the raw markup or the site's API, never from what WebFetch says about them. It also drops `<iframe>` embeds entirely ([Learnings](learnings.md)).

# Finding a site that serves one page for every URL

A host that has retired its archive, or that renders records in JavaScript,
answers every URL with the same shell - and each capture looks plausible on its
own. They are obvious in the aggregate:

```sql
SELECT COUNT(*), substring(min("url") from '^https?://(?:www\\.)?([^/]+)')
FROM "Source" WHERE length("text") > 500
GROUP BY md5("text") HAVING COUNT(*) > 2 ORDER BY 1 DESC;
```

Identical text across many distinct URLs means the fetch is worthless whatever
it says. Run this before fanning wiki work out: it found wpcentral.com and
clinicaltrials.gov, 38 links between them, which would otherwise have become 38
wikis about the wrong thing.
